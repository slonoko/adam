services:
  # Full MCP server with all tools
  adam-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - adam-network

networks:
  adam-network:
    external: true